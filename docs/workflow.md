Workflow
========

Setup:

- clone repository
- `npm install`

Compile all assets:

- `npm run mix` - styles
- `npm run pegjs` - parser

Run the app:

- `npm run start`

Run tests:

- `nmp run test`

Package the app:

- `npm run build`